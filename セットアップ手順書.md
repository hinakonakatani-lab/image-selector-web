# 画像選定ツール（Web版）セットアップ手順

## 全体の流れ

1. Google Cloud ConsoleでOAuth設定
2. GitHubにpush
3. VercelにデプロイしてKV（データベース）を作成
4. 環境変数を設定

---

## ① Google Cloud Console の設定

1. https://console.cloud.google.com/ を開く
2. プロジェクトを作成（または既存を選択）
3. 左メニュー →「APIとサービス」→「有効なAPIとサービス」
4. 「Google Drive API」を検索して有効化
5. 左メニュー → 「認証情報」→「認証情報を作成」→「OAuthクライアントID」
6. アプリの種類：「ウェブアプリケーション」
7. 承認済みリダイレクトURIに追加：
   - `http://localhost:3000/api/auth/callback/google`（開発用）
   - `https://あなたのVercelドメイン/api/auth/callback/google`（本番用）
8. 作成後、クライアントIDとシークレットをメモ

---

## ② GitHubにpushしてVercelにデプロイ

```bash
cd "/Users/mac114/Desktop/claude test/image-selector-web"
git init
git add .
git commit -m "初回コミット"
```

その後GitHubで新しいリポジトリを作成して、Vercelと連携。

---

## ③ Vercel KV（データベース）の作成

1. Vercelダッシュボード → プロジェクトを開く
2. 「Storage」タブ → 「Create Database」→「KV」を選択
3. 作成後、「Connect to Project」でこのプロジェクトに接続
4. 自動的に環境変数が設定される

---

## ④ Vercelの環境変数設定

Vercelダッシュボード → Settings → Environment Variables に以下を追加：

| 変数名 | 値 |
|---|---|
| `GOOGLE_CLIENT_ID` | Google Cloud Consoleのクライアント ID |
| `GOOGLE_CLIENT_SECRET` | Google Cloud Consoleのシークレット |
| `NEXTAUTH_SECRET` | ランダムな文字列（`openssl rand -base64 32`で生成） |
| `NEXTAUTH_URL` | https://あなたのVercelドメイン |

---

## ローカルで動かす場合

`.env.local` ファイルを編集して実際の値を入れる：

```
GOOGLE_CLIENT_ID=実際のクライアントID
GOOGLE_CLIENT_SECRET=実際のシークレット
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=ランダムな文字列
KV_URL=VercelのKV URL
...
```

その後：
```bash
npm run dev
```

http://localhost:3000 を開く
